---
title: VIM Cheatsheet
date: 2021-06-11
---

\代表<Leader>

:bN 跳转到 N 编号的 buffer

:ls 列出所有 buffer

“寄存器

:e#重新打开上一个文件

N ctrl+w+<减小窗口垂直大小

N ctrl+w+>增大窗口垂直大小

ctrl+w+=使所有窗口宽高等分

ctrl+w+s 分成上下窗口

ctrl+w+v 分成左右窗口

ctrl+p 调用 CtrlP 插件的快捷键

ctrl+i 前进光标位置记录

ctrl+o 回退光标位置记录

gf 跳转到 nodejs 的定义

gcc 注释

zf 折叠代码

zd 删除折叠

q:输入命令的历史记录

vim-multiple-cursors

ctrl+n 选词

ctrl+p 撤销选词

ctrl+x 跳过

NERDTree

i 上下分割窗口打开文件

s 左右分割窗口打开文件

A 最大化最小化窗口

I 切换显示隐藏文件

C 切换主目录到选中目录

vim-prettier

\p 执行 prettier

vim 键位绑定

- n: normal only
- v: visual and select
- o: operator-pending
- x: visual only
- s: select only
- i: insert
- c: command-line
- l: insert, command-line, regexp-search (and others. Collectively called "Lang-Arg" pseudo-mode)
